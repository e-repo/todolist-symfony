{% extends 'mail.html.twig' %}
{% block body %}
    <p>{% trans %}Sign Up confirm link:{% endtrans %}</p>
    {% set url = url('auth.signup_confirm', {'token': token}) %}
    <p><a href="{{ url }}">{{ url }}</a></p>
{% endblock %}