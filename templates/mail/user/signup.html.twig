{% extends 'mail.html.twig' %}
{% block body %}
    <p>{% trans from 'app' %}Sign Up confirm link:{% endtrans %}</p>
    {% set url = url('auth.signup.confirm', {'token': token}) %}
    <p><a href="{{ url }}">{{ url }}</a></p>
{% endblock %}